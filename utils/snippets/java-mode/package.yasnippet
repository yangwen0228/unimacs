# -*- mode: snippet -*-
# package ${1:`(mapconcat 'identity (cdr (last "java" (split-string default-directory "/" t))) ".")`};$0
# name: package
# key: pa
# --
`(let* ((dirname (directory-file-name (file-name-directory (buffer-file-name)))) (index (string-match "src/main/java" dirname))) (if (and index (< (+ index 13) (length dirname))) (concat "package " (subst-char-in-string ?/ ?. (substring dirname (+ index 14))) ";") "package ;"))`